[phase2_start.md](file;file:///Users/arpit/Desktop/dipika/codes/ai_playwright_lab/docs/phase2/phase2_start.md)  Create a requirement doc based on this file 'phase2_req.md' under the docs folder. 
Use python and uv for this.
For each sub project create a dedicated README.md file with details on what project is doing, what test cases it is solving and how to run the test cases and what each parameter in the test case does. 
Also do code commenting to make it easy to understand. 
Do not write any code. Just give the 'phase2_req.md' file. Keep the format similar to [req.md](ai_playwright_lab/docs/req.md) 

------------------
------------------

Now based on this create a phase2_architecture.md and phase2_plan.md files for this
Just give above files. Do not write any code as of now.
Keep the strucuture similar to [architecture.md](ai_playwright_lab/docs/architecture.md) and [plan.md](ai_playwright_lab/docs/plan.md) 

------------------

Now based on these break it down into smaller executable tasks which can be validated independently. Write the details in phase2_executable_plan.md
Use the format similar to 

------------------

Now based on these break it down into smaller executable tasks which can be validated independently. Write the details in phase2_executable_plan.md
Use the format similar to [executable_plan.md](ai_playwright_lab/docs/executable_plan.md) 

------------------

Update the AGENTS.md file based on [executable_plan.md](docs/executable_plan.md) file. 
Keep only the content which you think would be required before starting any task. Expectations would be keep generic info which can be used across tasks/ project.

------------------

for all the projects, if we are spawning our own servers, then add a command in respective readme files on how to spawn
that server individually. Currently its auto starting and stopping as part of test run. That is still required, but for
exploration of what exactly server looks like, I need to spin up server individually to explore it further.

------------------


